<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.together.mapper.ManagementMapper">

    <select id="findAll" resultType="com.project.together.domain.Rule">
        SELECT * FROM Rule
        ORDER BY rule_num DESC
        LIMIT #{start}, #{limit}
    </select>

    <select id="findRuleByRuleNum" parameterType="int" resultType="com.project.together.domain.Rule">
        SELECT * FROM Rule WHERE rule_num = #{ruleNum}
    </select>

   <!-- <select id="findPrevRuleByCurrentRuleDate" parameterType="String" resultType="com.project.together.domain.Rule">
        <![CDATA[
        SELECT * FROM Rule
        WHERE rule_date < #{ruleDate}
        ORDER BY rule_date DESC
        LIMIT 1
    ]]>
    </select>-->






</mapper>